httpd:
  port: 5099
  host: localhost
  request-logging: false
grpc:
  port: 2525
  host: localhost
database:
  file-based:
    directory: ./data  
logging:
  colored: true
  level: debug
  output: stdout
  report-caller: false
client-logging:
  colored: true
  level: error
  output: stdout
  report-caller: true
db:
  directory: "./data/badger"
